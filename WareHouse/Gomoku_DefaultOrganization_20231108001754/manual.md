manual.md

```
# Snake Game

A classic Snake game developed in Python using Pygame.

## Quick Install

Before you can run the game, you need to install the required dependencies.

If you have pip installed, you can install the dependencies by running:

`pip install -r requirements.txt`

If you're using conda, you can create a new environment and install the dependencies with:

`conda create --name <env> --file requirements.txt`

## 🤔 What is this?

This is a classic Snake game where the player controls a snake that grows in length as it consumes food. The game ends when the snake collides with itself or the border of the playing field. The goal is to consume as much food as possible before the game ends.

## 🎮 How to Play

- Use the arrow keys to control the direction of the snake.
- The snake cannot reverse direction 180 degrees. For example, if moving right, it cannot immediately move left.
- The snake grows in length each time it consumes food.
- The game ends when the snake collides with itself or the border of the playing field.

## 📖 Game Rules

- The snake moves in a continuous direction until the player inputs a change in direction.
- The snake grows in length each time it consumes food.
- The game ends when the snake collides with itself or the border of the playing field.
- The score increases each time the snake consumes food.

## 🚀 How to Run the Game

To run the game, navigate to the directory containing the game files in your terminal and run the following command:

`python main.py`

Enjoy the game!

```
